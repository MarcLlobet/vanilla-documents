import {
  ColorSlider as CurrentColorSlider,
  DarkModeButton as CurrentDarkModeButton,
} from ".";

export default {
  title: "Components/ThemeMode",
};

export const ColorSlider = () => CurrentColorSlider();

export const DarkModeButton = () => CurrentDarkModeButton();
